# Copy to .env.local for local dev (not committed).

# Strapi base URL — no trailing slash, no /api suffix (see src/lib/strapi.js).
NEXT_PUBLIC_STRAPI_API_URL=http://localhost:1337

# Optional: secret for POST /api/revalidate?secret=... (Strapi webhooks).
# REVALIDATION_TOKEN=
